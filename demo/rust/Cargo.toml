[package]
name = "pdf2img-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pdf2img-core = { path = "../../crates/pdf2img-core" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
